{
  "name": "server2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nexus/schema": "^0.13.1",
    "@prisma/client": "2.0.0-beta.1",
    "@types/bcrypt": "^3.0.0",
    "@types/jsonwebtoken": "^8.3.9",
    "@types/type-is": "^1.6.3",
    "apollo-server": "^2.12.0",
    "bcrypt": "^4.0.1",
    "csv-parse": "^4.8.8",
    "dotenv": "^8.2.0",
    "graphql": "^14.6.0",
    "graphql-middleware": "^4.0.2",
    "graphql-relay": "^0.6.0",
    "graphql-shield": "^7.2.4",
    "jsonwebtoken": "^8.5.1",
    "nexus-prisma": "^0.12.0"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.44",
    "@prisma/cli": "2.0.0-beta.1",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "setup": "prisma migrate save --experimental && prisma migrate up --experimental && ts-node prisma/seed.ts",
    "start": "node dist/server",
    "build": "npm -s run generate && tsc",
    "generate": "npm -s run generate:prisma && npm -s run generate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "ts-node --transpile-only src/schema",
    "postinstall": "npm -s run generate",
    "dev": "ts-node-dev --no-notify --respawn --transpileOnly src/server"
  }
}
