# Migration `20200326175052-init`

This migration has been generated by dev.mohe at 3/26/2020, 5:50:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "role" TEXT NOT NULL DEFAULT 'ADMIN' ,
    PRIMARY KEY ("id")
)

CREATE TABLE "quaint"."Student" (
    "class" TEXT NOT NULL DEFAULT '' ,
    "grade" INTEGER NOT NULL DEFAULT 0 ,
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "startNumber" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id")
)

CREATE TABLE "quaint"."Round" (
    "createdBy" TEXT NOT NULL  ,
    "id" TEXT NOT NULL  ,
    "student" TEXT NOT NULL  ,
    "time" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id"),FOREIGN KEY ("createdBy") REFERENCES "User"("id") ON DELETE RESTRICT ON UPDATE CASCADE,
FOREIGN KEY ("student") REFERENCES "Student"("id") ON DELETE RESTRICT ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "quaint"."User.name" ON "User"("name")

CREATE UNIQUE INDEX "quaint"."Student.startNumber" ON "Student"("startNumber")

CREATE UNIQUE INDEX "quaint"."Student.name" ON "Student"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200326175052-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id            String   @default(cuid()) @id
+  name          String   @unique
+  password      String
+  role          UserRole
+  createdRounds Round[]
+}
+
+enum UserRole {
+  ADMIN
+  TEACHER
+  VIEWER
+}
+
+model Student {
+  id          String @default(cuid()) @id
+  startNumber Int    @unique
+  name        String @unique
+  class       String
+  grade       Int
+  rounds      Round[]
+}
+
+model Round {
+  id        String   @default(cuid()) @id
+  student   Student
+  createdBy User
+  time      Int
+}
```
