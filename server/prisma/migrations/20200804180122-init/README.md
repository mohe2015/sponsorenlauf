# Migration `20200804180122-init`

This migration has been generated by mohe2015 <Moritz.Hedtke@t-online.de> at 8/4/2020, 6:01:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('ADMIN', 'TEACHER', 'VIEWER');

CREATE TABLE "public"."User" (
"identifier" SERIAL,
"name" text  NOT NULL ,
"hashedPassword" text  NOT NULL ,
"role" "UserRole" NOT NULL ,
PRIMARY KEY ("identifier"))

CREATE TABLE "public"."Runner" (
"identifier" SERIAL,
"name" text  NOT NULL ,
"clazz" text  NOT NULL ,
"grade" integer  NOT NULL ,
PRIMARY KEY ("identifier"))

CREATE TABLE "public"."Round" (
"identifier" SERIAL,
"studentId" integer  NOT NULL ,
"createdById" integer  NOT NULL ,
"time" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("identifier"))

CREATE TABLE "public"."UserSession" (
"id" text  NOT NULL ,
"userId" integer  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.name_unique" ON "public"."User"("name")

CREATE UNIQUE INDEX "Runner.name_unique" ON "public"."Runner"("name")

ALTER TABLE "public"."Round" ADD FOREIGN KEY ("createdById")REFERENCES "public"."User"("identifier") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Round" ADD FOREIGN KEY ("studentId")REFERENCES "public"."Runner"("identifier") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."UserSession" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("identifier") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200804180122-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,47 @@
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
+datasource postgresql {
+  provider = "postgresql"
+  url = "***"
+}
+
+model User {
+  identifier      Int           @id @default(autoincrement())
+  name            String        @unique
+  hashedPassword  String
+  role            UserRole
+  createdRounds   Round[]
+  currentSessions UserSession[]
+}
+
+model Runner {
+  identifier  Int     @id @default(autoincrement())
+  name        String  @unique
+  clazz       String
+  grade       Int
+  rounds      Round[]
+}
+
+model Round {
+  identifier  Int      @id @default(autoincrement())
+  studentId   Int
+  createdById Int
+  time        DateTime @default(now())
+  createdBy   User     @relation(fields: [createdById], references: [identifier])
+  student     Runner   @relation(fields: [studentId], references: [identifier])
+}
+
+model UserSession {
+  id         String   @id
+  userId     Int
+  createdAt  DateTime @default(now())
+  user       User     @relation(fields: [userId], references: [identifier])
+}
+
+enum UserRole {
+  ADMIN
+  TEACHER
+  VIEWER
+}
```


