# Migration `20200330232856-init`

This migration has been generated by dev.mohe at 3/30/2020, 11:28:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "role" TEXT NOT NULL DEFAULT 'ADMIN' ,
    PRIMARY KEY ("id")
)

CREATE TABLE "quaint"."Student" (
    "class" TEXT NOT NULL DEFAULT '' ,
    "grade" INTEGER NOT NULL DEFAULT 0 ,
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "startNumber" INTEGER NOT NULL DEFAULT 0 PRIMARY KEY AUTOINCREMENT
)

CREATE TABLE "quaint"."Round" (
    "createdBy" TEXT NOT NULL  ,
    "id" TEXT NOT NULL  ,
    "student" TEXT NOT NULL  ,
    "time" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id"),FOREIGN KEY ("createdBy") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("student") REFERENCES "Student"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "quaint"."User.name" ON "User"("name")

CREATE UNIQUE INDEX "quaint"."Student.startNumber" ON "Student"("startNumber")

CREATE UNIQUE INDEX "quaint"."Student.name" ON "Student"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200330232856-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id            String   @default(cuid()) @id
+  name          String   @unique
+  password      String
+  role          UserRole
+  createdRounds Round[]
+}
+
+enum UserRole {
+  ADMIN
+  TEACHER
+  VIEWER
+}
+
+model Student {
+  id          String @default(cuid()) @id
+  startNumber Int    @unique @default(autoincrement()) // https://github.com/prisma/prisma2/issues/1938 https://github.com/graphql-nexus/nexus-plugin-prisma https://github.com/prisma/prisma2 has a bug autoincrement makes this the primary key
+  name        String @unique
+  class       String
+  grade       Int
+  rounds      Round[]
+}
+
+model Round {
+  id        String   @default(cuid()) @id
+  student   Student
+  createdBy User
+  time      Int
+}
```
